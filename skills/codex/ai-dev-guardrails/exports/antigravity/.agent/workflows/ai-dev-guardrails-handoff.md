---
description: Create a handoff markdown file under docs/ai_journal/ (includes git state + changed files).
---

# Handoff (AI Dev Guardrails)

Before ending a session, run:

`python .agent/skills/ai-dev-guardrails/scripts/ai_journal.py handoff --note "<next step>" --note "<blocker>" --note "<verification>"`

Also ensure `docs/current_task.md` has updated Plan checkboxes and a concrete Handoff Note.

