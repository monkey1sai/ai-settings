# fastapi - Codex CLI Installation Guide

## Quick Install

```bash
# Extract to user skills directory (recommended)
unzip fastapi-codex.zip -d ~/.codex/skills/fastapi/

# Restart Codex CLI to detect new skill
codex
```

## Project-Level Install

```bash
# Extract to project directory (only available in this project)
unzip fastapi-codex.zip -d ./.codex/skills/fastapi/
```

## Verify Installation

1. Start Codex CLI: `codex`
2. Type `$` to see available skills
3. You should see `fastapi` in the list

## Usage

### Explicit Invocation
- Type `$fastapi` to invoke the skill directly
- Or use `/skills` command to browse available skills

### Implicit Invocation
Codex will automatically activate this skill when your task matches the skill description.

## Skill Locations (Precedence Order)

1. `$CWD/.codex/skills/` - Current directory
2. `$REPO_ROOT/.codex/skills/` - Repository root
3. `~/.codex/skills/` - User home (recommended)
4. `/etc/codex/skills/` - System-wide

## Reference

- [Codex CLI Skills Documentation](https://developers.openai.com/codex/skills/)

---
*Generated by Skill Seekers*
